{{> head }}
{{> menu }}

<div class="row">
    <div class="col">
        <h1>Servicios:</h1>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-10"></div>
        <div class="col-2">
            <a href="/nuevo-servicio"><input type="submit" value="Nuevo Servicio" class="btn btn-primary"></a>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <table class="table">
        <thead>
          <tr>
            <th scope="col" class="col-2">#</th>
            <th scope="col" class="col-6">Categoría</th>
            <th scope="col" class="col-2">Editar</th>
            <th scope="col" class="col-2">Eliminar</th>
          </tr>
        </thead>
        <tbody>
          {{#if servicios.length}}
            {{#each servicios}}
              <tr>
                <th scope="row">1</th>
                <td>{{titulo}}</td>
                <td><a href="/servicios/editar/{{_id}}"><input type="button" value="Editar" class="btn btn-primary"></a></td>
                <td><a href="/servicios/borrar/{{_id}}"><input type="button" value="Eliminar" class="btn btn-primary"></a></td>
              </tr>
            {{/each}}
          {{/if}}
        </tbody>
      </table>
        </div>
    </div>
</div>

{{> footer}}